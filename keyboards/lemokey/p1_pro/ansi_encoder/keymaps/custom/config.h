#pragma once

/* Слой 0 хранится полными цветами (81 × 3 байта) — его красит лаунчер Keychron.
 * Слои 1-3 хранятся номерами из палитры, по полбайта на светодиод.
 * Итого: 1 (флаги слоёв) + 1 (счётчик палитры) + 48 (палитра) + 243 + 123. */
#define EECONFIG_USER_DATA_SIZE 416

/* Caps Word включается нажатием обоих Shift разом и гаснет сам на пробеле.
 * Обычный Caps Lock не трогаем — он остаётся для целых предложений. */
#define BOTH_SHIFTS_TURNS_ON_CAPS_WORD
